# python -m pip install -r tools/requirements.txt
#
# Pinned to a major version: patches and fixes still arrive, the breaking change
# does not. Same compromise as the platform pins in platformio.ini. Raise a floor
# when you actually need something newer, and say why in the commit.

pyserial~=3.5       # capture.py
matplotlib~=3.11    # plot.py
pandas~=3.0         # plot.py
