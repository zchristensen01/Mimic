// units.h: Stage 0, S6. Counts, radians and duty, converted in one place.
// Declarations only. See Stage 0 Workbook, S6.

#pragma once

// TODO (S1/S3): your MEASURED figure. Not derived, and not copied from anywhere.
// TODO: constexpr float COUNTS_PER_OUTPUT_REV = <measured>;
// TODO: constexpr float RAD_PER_COUNT         = 6.2831853f / COUNTS_PER_OUTPUT_REV;
// TODO: constexpr float RAD_TO_DEG            = 57.2957795f;

// TODO: inline float countsToRad(long c);
// TODO: inline long  radToCounts(float r);

// TODO: radToCounts() truncates toward zero with a plain cast. Decide truncation
//       or rounding, and write down which and why.
